<% include ../partials/header %>
<% include ../partials/navbar %>

<div class="ui main text raised container">
  <div class="ui top attached segment">
    <h1 class="ui clearing left floated header">Edit Comment</h1>
    <form class="ui form" action="/galleries/<%= gallery_id %>/comments/<%= comment._id %>?_method=PUT" method="POST">
      <div class="field">
        <label>Comment</label>
        <textarea type="text" name="comment[text]"><%= comment.text %></textarea>
      </div>
      <button class="ui button" type="submit">Submit</button>
      <a class="ui button" href="/galleries">Cancel</a>
    </form>
  </div>
</div>

<% include ../partials/footer %>
